apiVersion: v1
kind: Service
metadata:
  name: test-service
  labels:
    app.kubernetes.io/name: test-service
spec:
  selector:
    app: test-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: "80"
