import { getComposatorMetadata } from 'tests/resources/metadata/getKubernatorMetadata';

describe('Test KubernetesMetadata', () => {
  describe('Test methods', () => {
    describe('Test method: validate', () => {
      it('Should return true on valid metadata', () => {
        return True ;
      });
    });
  });
});